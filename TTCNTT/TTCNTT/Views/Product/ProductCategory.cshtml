@model ProductViewModel
@inject TTCNTT.Efs.Context.WebTTCNTTContext context
@using X.PagedList.Mvc.Core;
@using X.PagedList;

@{
    ViewBag.Title = Model.category.Name;
}


<section class="banner-sec">
    <div class="main-banner inner" style=" background:url(/Image/@Model.setting.IMAGESLIDE) no-repeat top center fixed;">
        <a href="#" name="scroll-top-div"></a>
        <div class="overlay-mask">
            <!-- Start:Slider area -->
            <div class="container">
                <h1>@Model.setting.PRODUCT</h1>
                <h6>@Model.setting.PRODUCT_SLOGAN</h6>
                <hr>
            </div>
            <!-- End:Slider area -->
        </div>
    </div>
</section>

<section class="blog-outer-area">
    <div class="content-area">
        <div class="blog-area-sec">
            <!-- Start: Blog Sec -->
            <div class="container">
                <div class="col-md-12">
                    <div class="main-title">
                        <h5>@Model.category.Name</h5>
                        <h2>@Model.setting.PRODUCT_CATEGORY_SLOGAN</h2>
                    </div>
                </div>
            </div>

            <div class="blog-sec">
                <div class="container">
                    <!-- Start:Blog Area -->
                    <div class="blog-main-area">
                        <!-- Start:Blog Items (With Mini Grid) -->
                        <div class="mini-grid-area">
                            @foreach (var item in ViewBag.OnePageOfProducts)
                            {
                                <div class="col-md-4 ">
                                    <div class="item-box">
                                        <a href="~/san-pham/chi-tiet/@item.Slug_Name" class=" img-holder">
                                            <img src="~/Image/@item.ImageSlug" alt="">
                                            <div class="mask"></div>
                                        </a>
                                        <div class="info-text-holder">
                                            <h6><a href="~/san-pham/chi-tiet/@item.Slug_Name">@item.Name.</a></h6>
                                            <span><a href="~/san-pham/chi-tiet/@item.Slug_Name">@item.CreatedDate</a></span>
                                            <p>@item.ShortDescription_Html. </p>
                                            <a href="~/san-pham/chi-tiet/@item.Slug_Name" class="button-medium">XEM THÊM</a>
                                        </div>
                                    </div>
                                </div>
                            }

                        </div>
                        <!-- End:Blog Items (With Mini Grid) -->
                        <!-- Start:Pagination -->
                        <div class="pagination-area">
                            <ul class="pagination">

                                @Html.PagedListPager((IPagedList)ViewBag.OnePageOfProducts, page => Url.Action("loai-san-pham", "san-pham", new { id = Model.category.Slug_Name, page }))

                            </ul>
                        </div>
                        <!-- End:Pagination -->

                    </div>
                    <!-- End:Blog Area -->
                </div>
            </div>
            <!-- End: Blog Sec -->

        </div>
    </div>
</section>